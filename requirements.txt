# requirements.txt - Kuikma Chess Engine Dependencies

# Core Dependencies
streamlit>=1.32.0
pandas>=2.2.0
numpy>=1.24.0
python-chess>=1.999

# Database
sqlite3  # Built-in with Python

# Visualization
plotly>=5.18.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Data Processing
scipy>=1.12.0
python-dateutil>=2.8.2

# Web and File Processing
requests>=2.31.0
weasyprint>=61.0  # For PDF generation (optional)

# Enhanced Features
cffi>=1.15.0  # For weasyprint support

# Development Dependencies (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# Optional: Enhanced PDF generation
# reportlab>=4.0.0

# Platform-specific dependencies for weasyprint
# Windows users may need:
# - Microsoft Visual C++ Build Tools
# - GTK3 runtime

# Linux users may need:
# sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info

# macOS users may need:
# brew install cairo pango gdk-pixbuf libffi
